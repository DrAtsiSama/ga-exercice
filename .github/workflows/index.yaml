name: Correction

on:
  # 1.2
  # push:
  #   branche:
      # - dev
  # 1.3
  # push:
  #   branches:
  #    - '!staging'
  # 1.4
  # push:
  #   branches:
  #     - 'feat/**'
  push:
    branches:
      - '**.ts'
jobs: 
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3